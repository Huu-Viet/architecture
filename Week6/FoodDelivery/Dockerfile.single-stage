# ⚠️ SINGLE-STAGE VERSION (Inefficient - for comparison)
# This builds everything in one stage without optimization
FROM node:20-alpine
WORKDIR /app
ENV NODE_ENV=production

COPY package.json ./
RUN npm install

COPY . .
RUN npm run build

EXPOSE 3000
CMD ["node", "dist/apps/api/main"]
